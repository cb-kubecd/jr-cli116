buildPack: go-cli
pipelineConfig:
  pipelines:
    release:
      pipeline:
        stages:
          - name: foo
            options:
              containerOptions:
                volumeMounts:
                - mountPath: /var/build-secrets/knative-git-user-pass
                  name: secret-volume-knative-git-user-pass-fj2tb
